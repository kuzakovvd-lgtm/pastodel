<section class="hero-section">
  <div class="container hero-section__grid">
    <div class="hero-section__content">
      <p class="hero-section__eyebrow">ЗАМОРОЖЕННАЯ ГОТОВАЯ ПАСТА</p>
      <h1>Готовые пасты — удобный ужин на каждый день</h1>
      <p class="hero-section__subhead">
        Порция 400 г, знакомые вкусы и простой сценарий приготовления дома. Разогреть и подать на стол за 6–7 минут.
      </p>
      <div class="pd-hero-audience">
        <p class="pd-hero-audience__item"><strong>Для покупателей:</strong> вся линейка и карточки товаров с описанием вкуса, состава и способа приготовления.</p>
        <p class="pd-hero-audience__item"><strong>Для торговых сетей:</strong> отдельный маршрут к ассортименту, заявке и условиям запуска.</p>
      </div>

      <div class="hero-section__actions">
        <a class="button" href="/partneram/">Для сетей</a>
        <a class="button button--secondary" href="/katalog/">Продукция</a>
      </div>

      <div class="pd-hero-cta-notes">
        <span class="pd-hero-cta-note">Быстрый путь к заявке и условиям сотрудничества</span>
        <span class="pd-hero-cta-note">Смотреть вкусы и составы</span>
      </div>
    </div>

    <div class="hero-visual" aria-label="Визуальная композиция продукта Pastodel">
      <div class="hero-visual__frame">
        <div class="hero-visual__glow" aria-hidden="true"></div>
        <div class="hero-visual__plate" aria-hidden="true">
          <img
            src="/images/products/mak-end-chiz.jpg"
            alt="Готовая паста Pastodel на светлой сервировке"
            width="720"
            height="720"
            loading="eager"
            fetchpriority="high"
          />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-section {
    padding: clamp(30px, 4vw, 54px) 0 clamp(76px, 8vw, 114px);
  }

  .hero-section__grid {
    display: grid;
    gap: 34px;
    align-items: center;
  }

  .hero-section__content {
    display: grid;
    gap: 20px;
    max-width: 36rem;
  }

  .hero-section__eyebrow {
    margin: 0;
    color: var(--brand-accent);
    font-size: 0.9rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .hero-section h1 {
    max-width: 8.8ch;
    line-height: 0.94;
    letter-spacing: -0.045em;
    text-wrap: balance;
  }

  .hero-section__subhead {
    max-width: 24ch;
    color: var(--text-primary);
    font-size: clamp(1.15rem, 1.95vw, 1.42rem);
    line-height: 1.48;
  }

  .hero-section__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    padding-top: 8px;
  }

  .hero-section__actions :global(.button:first-child) {
    min-width: 174px;
  }

  .hero-section__actions :global(.button--secondary) {
    min-width: 164px;
  }

  .hero-visual__frame {
    position: relative;
    overflow: hidden;
    min-height: 590px;
    padding: 34px;
    border: 1px solid var(--border);
    border-radius: 34px;
    background:
      radial-gradient(circle at 18% 16%, rgba(212, 130, 84, 0.12), transparent 24%),
      radial-gradient(circle at 84% 18%, rgba(255, 253, 249, 0.9), transparent 22%),
      linear-gradient(180deg, rgba(255, 253, 249, 0.88), rgba(245, 236, 225, 0.98));
    box-shadow: 0 24px 48px rgba(36, 28, 24, 0.07);
  }

  .hero-visual__glow {
    position: absolute;
    inset: auto auto 40px 48px;
    width: 210px;
    height: 210px;
    border-radius: 50%;
    background: rgba(212, 130, 84, 0.16);
    filter: blur(10px);
  }

  .hero-visual__plate {
    position: absolute;
    inset: 42px 62px 60px 106px;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background:
      radial-gradient(circle at center, #fffdfa 0 47%, #fbf4eb 48% 54%, #ead8c6 55% 64%, rgba(212, 130, 84, 0.16) 65% 67%, transparent 68%),
      radial-gradient(circle at 50% 46%, rgba(255, 255, 255, 0.52), transparent 42%),
      radial-gradient(circle at center, rgba(212, 130, 84, 0.09), rgba(212, 130, 84, 0.02));
    box-shadow:
      inset 0 0 0 1px rgba(255, 255, 255, 0.65),
      0 26px 54px rgba(90, 61, 39, 0.12);
  }

  .hero-visual__plate img {
    width: min(100%, 442px);
    height: auto;
    object-fit: contain;
    filter: saturate(1.06) drop-shadow(0 24px 28px rgba(76, 46, 25, 0.14));
  }

  @media (min-width: 960px) {
    .hero-section__grid {
      grid-template-columns: 44fr 56fr;
    }
  }

  @media (max-width: 959px) {
    .hero-section__grid {
      gap: 26px;
    }

    .hero-visual__frame {
      min-height: 520px;
    }

    .hero-visual__plate {
      inset: 52px 42px 126px;
    }
  }

  @media (max-width: 719px) {
    .hero-section {
      padding-top: 20px;
      padding-bottom: 68px;
    }

    .hero-section__content {
      gap: 14px;
    }

    .hero-section h1 {
      max-width: 9.2ch;
      font-size: clamp(2.625rem, 11vw, 3rem);
    }

    .hero-section__actions {
      flex-direction: column;
      gap: 12px;
    }

    .hero-section__actions :global(.button) {
      width: 100%;
      min-height: 52px;
    }

    .hero-visual__frame {
      min-height: auto;
      padding: 18px;
      display: grid;
      gap: 12px;
    }

    .hero-visual__glow {
      left: auto;
      right: -16px;
      bottom: 44px;
      width: 124px;
      height: 124px;
    }

    .hero-visual__plate {
      position: relative;
      inset: auto;
      min-height: 286px;
    }
  }
</style>
